# Daily health report — 2026-09-11

**Status:** UNKNOWN — live checks blocked, could not verify site/sync health today.

## What changed since the last report
No prior report exists — this is the first one. Baseline only.

## Needs attention
1. **Live checks blocked, not skipped.** The sandbox's egress proxy denied all
   outbound requests today (`connect_rejected`, policy denial) — couldn't reach:
   - `https://secretshopdota.co.uk/api/imprint-sync` (standings sync health)
   - `https://secretshopdota.co.uk/` and `/playoffs` (site up/content check)
   - the playoffs Google Sheet CSV export (header-drift check)
   None of these were tested. Treat today as **no data**, not "healthy" — worth
   checking manually or re-running once egress is available.
2. **No code smells found** in the repo itself: no new TODOs/FIXMEs, and no
   `.sql` migration file has changed recently (newest is `standings-migration.sql`
   family, last touched 2026-09-04, well before the current code).
3. **Baseline file sizes** (for future diffing, not a problem today):
   `js/standings.js` 1420 lines, `js/playoffs.js` 818 lines — both already over
   the ~600-line flag threshold. Future reports should flag growth, not the
   existing size.

## Next report
Re-run the live checks (imprint-sync JSON, homepage, /playoffs, sheet CSV headers)
as soon as network access works, and compare code/migration state against this
baseline.
