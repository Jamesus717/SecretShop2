# Daily health report — 2026-09-14

**Status:** UNKNOWN — live checks blocked a fourth day running. No visibility into site/sync health.

## What changed since the last report
- **Nothing in the repo.** Zero commits since yesterday's report (`17cabb5`). No code,
  no migrations, no file-size changes (`standings.js` 1420, `playoffs.js` 818 lines,
  unchanged). No new TODOs.
- **Live checks blocked again — fourth day in a row.** Same egress denial as the last
  three reports, now confirmed at the proxy level (`connect_rejected`, org policy),
  not just a WebFetch quirk — tried both WebFetch and a direct `curl` through the
  configured proxy, same result on all hosts:
  - `secretshopdota.co.uk` (homepage, `/playoffs`, `/api/imprint-sync`)
  - `docs.google.com` (playoffs schedule CSV)
  Still no way to know whether Imprint sync is draining, whether the site is up, or
  whether the schedule sheet's headers are intact.

## Needs attention
1. **Four days with zero real visibility.** This is now clearly a standing egress
   policy block on this scheduled task's environment, not a transient network blip.
   The report will keep saying "unknown" every day until the egress policy for this
   session/environment is changed to allow `secretshopdota.co.uk` and
   `docs.google.com`. Worth fixing outside the repo, or checking site health manually
   in the meantime.
2. **No repo-side issues to report.** No new commits, no migration files touched, no
   file-size growth beyond baseline.

## Next report
Re-run live checks (imprint-sync JSON, homepage, `/playoffs`, sheet CSV headers) —
this has now failed identically for 4 straight days, so treat further identical
failures as expected until the egress policy is changed, not new information.
