# Daily health report — 2026-09-15

**Status:** UNKNOWN — live checks blocked a fifth day running. No visibility into site/sync health.

## What changed since the last report
- **Nothing in the repo.** Zero commits since yesterday's report (`20a180c`). No code,
  no migrations, no file-size changes (`standings.js` 1420, `playoffs.js` 818 lines,
  unchanged). No new TODOs.
- **Live checks blocked again — fifth day in a row.** Same egress denial as the last
  four reports, confirmed at the proxy level (`connect_rejected`, org policy) via both
  WebFetch and direct `curl`, on all hosts checked:
  - `secretshopdota.co.uk` (homepage, `/playoffs`, `/api/imprint-sync`)
  - `docs.google.com` (playoffs schedule CSV)
  Still no way to know whether Imprint sync is draining, whether the site is up, or
  whether the schedule sheet's headers are intact.

## Needs attention
1. **Five days with zero real visibility.** This is an egress policy block on this
   scheduled task's environment, not a transient network blip — it has not budged
   across five identical checks. The report will keep saying "unknown" every day
   until the egress policy for this session/environment allows
   `secretshopdota.co.uk` and `docs.google.com`. Worth checking site health manually
   in the meantime, since this routine currently can't.
2. **No repo-side issues to report.** No new commits, no migration files touched, no
   file-size growth beyond baseline.

## Next report
Re-run live checks (imprint-sync JSON, homepage, `/playoffs`, sheet CSV headers) —
this has now failed identically for 5 straight days, so treat further identical
failures as expected until the egress policy is changed, not new information.
