# Daily health report — 2026-09-16

**Status:** UNKNOWN — live checks blocked a sixth day running. No visibility into site/sync health.

## What changed since the last report
- **Nothing.** Zero commits since yesterday's report (`7fa6f0d`) — confirmed via
  `git diff --stat`, no files touched at all. No code, no migrations, no TODOs.
- **Live checks blocked again — sixth day in a row**, same `connect_rejected` /
  org policy denial at the proxy level, on both hosts:
  - `secretshopdota.co.uk` (homepage, `/playoffs`, `/api/imprint-sync`)
  - `docs.google.com` (playoffs schedule CSV)
  Still no way to know whether Imprint sync is draining, whether the site is up, or
  whether the schedule sheet's headers are intact.

## Needs attention
1. **Six days with zero real visibility.** This is an environment-level egress
   policy block, not a transient blip — identical every day since it first
   appeared. Nothing will change here until the egress policy for this
   scheduled task allows `secretshopdota.co.uk` and `docs.google.com`. Worth
   checking site health manually in the meantime.
2. **No repo-side issues.** No commits, no migration files touched, no file
   growth.

## Next report
Same checks again — treat another identical block as expected, not new
information, until the policy changes or someone confirms it's fixed.
